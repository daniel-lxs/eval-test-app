# Review target

This file exists to exercise Review Code automation.

```js
function addNumbers(a, b) {
  // BUG: string concatenation risk is not handled
  return a + b;
}
module.exports = { addNumbers };
```
